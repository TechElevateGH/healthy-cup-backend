version: "3.9"

services:
  app:
    build: .
    ports:
      - "5001:5000"
    depends_on:
      - db
    env_file:
      - .env 
      - .flaskenv
    
  db:
    image: postgres:13-alpine
    env_file:
      - .env
    